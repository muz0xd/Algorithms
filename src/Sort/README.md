There are some operations to send EOF in Eclipse console:
after input needed values click to another window then go back and enter Ctrl-Z for windows or Ctrl-D for Linux.

This is a known bug in Eclipse.